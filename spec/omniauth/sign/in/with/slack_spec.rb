# frozen_string_literal: true

RSpec.describe Omniauth::Sign::In::With::Slack do
  it "has a version number" do
    expect(Omniauth::Sign::In::With::Slack::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
